import React = require("react")
import ActionButton from "../base/ActionButton"

export default function() {
  return (
    <div>
      <ActionButton type="primary">Start Hosting</ActionButton>
    </div>
  )
}
